# MAPS — Material Allocation & Planning System

**Система автоматизированного распределения материалов для строительно-монтажных работ**

---

## Содержание

1. [Что делает система](#1-что-делает-система)
2. [Как работает алгоритм — 5 слоёв](#2-как-работает-алгоритм--5-слоёв)
3. [Аварийные работы — особый режим](#3-аварийные-работы--особый-режим)
4. [Архитектура и структура проекта](#4-архитектура-и-структура-проекта)
5. [Технологический стек](#5-технологический-стек)
6. [Быстрый старт](#6-быстрый-старт)
7. [Подробная инструкция по установке](#7-подробная-инструкция-по-установке)
8. [Веб-интерфейс и API](#8-веб-интерфейс-и-api)
9. [Формат входных данных Excel](#9-формат-входных-данных-excel)
10. [Команды CLI](#10-команды-cli)
11. [Структура базы данных](#11-структура-базы-данных)
12. [Результаты работы системы](#12-результаты-работы-системы)
13. [Разработка и тестирование](#13-разработка-и-тестирование)
14. [Дорожная карта](#14-дорожная-карта)
15. [Частые вопросы](#15-частые-вопросы)

---

## 1. Что делает система

MAPS решает задачу: **кому и откуда дать материалы при ограниченных ресурсах**.

Представьте ситуацию:
- 500 строительных работ требуют материалы
- Часть материалов уже **списана** на конкретные работы в SAP
- Часть **выдана физически**, но ещё не списана документально
- На складах есть партии разных дат и цен
- Часть материалов ещё «в пути» (подтверждённые поставки)
- Некоторых материалов не хватает (дефицит — к закупу)
- Некоторые работы — **аварийные** и требуют немедленного обеспечения
- Работы имеют разные приоритеты и сроки начала

Вручную это решить за разумное время невозможно. MAPS автоматически:

| Задача | Результат |
|--------|-----------|
| Анализирует списания (WriteOff) из SAP | Сколько уже «закрыто» документально |
| Анализирует выданное не списанное | Сколько фактически на руках у рабочих |
| Ищет материалы на складах своего филиала | Соблюдает FIFO и приоритеты |
| Резервирует из поставок своего филиала | Если склада не хватает |
| Фиксирует дефицит (к закупу) | Список позиций с оценочной стоимостью |
| Рассчитывает обеспечённость по стоимости | % покрытия = закрытая стоимость / (потребность × прогнозная цена) |
| Показывает возможные перемещения | Склады других филиалов, откуда можно привезти |

**Результат** — Excel-отчёт на 4 листах, доступный как через CLI, так и через браузер.

---

## 2. Как работает алгоритм — 5 слоёв

Для каждой потребности (работа + материал + количество) алгоритм последовательно проходит 5 слоёв. Следующий слой активируется только если предыдущий не закрыл потребность полностью.

### Порядок обработки работ

Прежде чем перейти к слоям, важно понять **в каком порядке** работы получают материал.

**Аварийные работы** — всегда первыми, только по дате начала:
```
Аварийная WO-AV-001 | начало 01.06.2026  ← обрабатывается 1-й
Аварийная WO-AV-002 | начало 15.06.2026  ← обрабатывается 2-й
```

**Плановые работы** — после аварийных, по дате начала и приоритету:
```
Приоритет 1 | начало 01.06.2026  ← обрабатывается 3-м
Приоритет 1 | начало 15.06.2026  ← обрабатывается 4-м
Приоритет 2 | начало 01.07.2026  ← обрабатывается 5-м
```

**Почему это важно?** При нехватке материала работа, обрабатываемая раньше, забирает остаток. Порядок — это фактически «очередь» за дефицитными ресурсами.

---

### Слой 1 — Списание (WriteOff)

**Что это?** Документально подтверждённые списания из SAP. Материал уже «закрыт» в системе — он физически израсходован и проведён через бухгалтерию.

**Источник данных:** файл `writeoffs.xlsx`, команда `import-writeoffs`.

```
Работа WO-001 | Кабель ВВГ 3х6
  Документ СП-000001 | списано 50 м | 01.03.2026
  Документ СП-000002 | списано 30 м | 10.03.2026
  Итого по слою 1: покрыто 80 м (из потребности 150 м)
  Остаток: 70 м → передаётся в слой 2
```

**Важно:** этот слой только читает данные — остатки складов НЕ меняются, потому что материал уже израсходован ранее.

---

### Слой 2 — Выдано не списано (IssuedNotWrittenOff)

**Что это?** Материал физически выдан рабочим (они его уже держат в руках), но бухгалтерское списание ещё не проведено. Часто это ситуация на конец периода.

**Источник данных:** файл `issued.xlsx`, команда `import-issued`.

```
Работа WO-001 | Кабель ВВГ 3х6
  Выдано 15.03.2026: 40 м (склад SKL-ALM-01, инфо)
  Итого по слою 2: покрыто 40 м
  Остаток: 30 м → передаётся в слой 3
```

**Важно:** этот слой тоже только читает данные — остатки складов НЕ уменьшаются, потому что физически материал уже ушёл в работу.

---

### Слой 3 — Склад своего завода/филиала

**Что это?** Живые остатки на складах. Алгоритм ищет партии, применяет FIFO и резервирует.

**FIFO — First In First Out (первым пришёл, первым ушёл):**
```
Партия A: поступила 01.01.2026, 100 м → используется первой  (старее)
Партия B: поступила 15.02.2026, 200 м → используется второй  (новее)
```

**Зачем FIFO?** Старые партии лежат дольше, у них выше риск порчи или истечения срока годности. Стандарт бухгалтерского учёта (МСФО/РСБУ).

**Приоритет складов внутри слоя 3:**
```
Сначала: склады того же ЗАВОДА (Plant), что и работа
Потом:   склады того же ФИЛИАЛА (другой завод)
```

```
Работа WO-001 | Завод KZ01 | Филиал Алматы
  SKL-KZ01-01 (завод KZ01) ← приоритет 1, взяли 30 м
  SKL-ALM-02  (филиал Алматы, завод KZ02) ← приоритет 2, нет остатков
  Итого по слою 3: покрыто 30 м
  Остаток: 0 м ✓ ПОТРЕБНОСТЬ ЗАКРЫТА
```

**После слоя 3 остатки складов УМЕНЬШАЮТСЯ** — это реальное резервирование.

---

### Слой 3б — Возможное перемещение (другие филиалы)

Параллельно с обработкой слоя 3 алгоритм проверяет склады **других филиалов**. Он НЕ резервирует материал (не уменьшает остатки), а только **фиксирует информацию** для аналитики:

```
Работа WO-001 | Филиал Алматы — не хватает 50 м кабеля
  → Склад SKL-AST-01 (Астана): есть 80 м  [ВОЗМОЖНОЕ, не резервируется]
  → В отчёт попадает в Лист 4 «Возможное перемещение»
```

Руководитель видит: «если привезти кабель из Астаны — можно закрыть эту работу».

---

### Слой 4 — Поставки (по филиалу)

**Что это?** Материалы «в пути» — подтверждённые или уже отгруженные поставки, которые придут на склад своего филиала.

**Условие:** поставка должна быть адресована тому же **филиалу**, что и работа.

```
Работа WO-002 | Филиал Астана | Труба 219×10
  Поставка ДОГ-2026-003 (филиал Астана) | ожидается 01.06.2026 | 200 м → резервируем 200 м
  Поставка ДОГ-2026-007 (филиал Астана) | ожидается 15.06.2026 | 100 м → резервируем 50 м
  Итого по слою 4: покрыто 250 м (из остатка 250 м)
  Остаток: 0 м ✓ ПОТРЕБНОСТЬ ЗАКРЫТА
```

---

### Слой 5 — К закупу (Дефицит)

**Что это?** Если после всех 4 слоёв потребность не закрыта — создаётся запись дефицита. Это список позиций, которые нужно закупать.

```
Работа WO-003 | Клапан 1" PN16 | нужно 10 шт
  Слои 1-4: покрыто 0 шт
  → Дефицит: 10 шт | оценочная стоимость: 450 000 тг (10 × 45 000 прогнозная цена)
```

**Прогнозная цена** (`prognosnaya_tsena`) загружается из файла потребностей и используется для оценки стоимости дефицита.

---

### Расчёт обеспечённости

```
Обеспечённость (%) = Закрытая стоимость / (Потребность × Прогнозная цена) × 100

Где:
  Закрытая стоимость = сумма по слоям 1-4 (кол-во × фактическая цена)
  Потребность × Прогнозная цена = плановая стоимость всей потребности
```

Если прогнозная цена не задана (= 0), обеспечённость считается по количеству:
```
Обеспечённость (%) = Закрытое количество / Потребность × 100
```

---

### Ключевые гарантии алгоритма

| Гарантия | Как обеспечивается |
|----------|--------------------|
| Нет отрицательных остатков | `allocate = min(remaining, available)` |
| Нет превышения потребности | `remaining -= allocated` после каждого шага |
| Нет двойного распределения | Кэш доступности в памяти (`_batch_cache`, `_supply_cache`) |
| FIFO внутри склада | Сортировка партий по `data_postupleniya ASC` |
| Повторный запуск — те же результаты | `reset_dostupno()` восстанавливает доступность перед каждым запуском |
| Слои 1-2 не меняют склад | Только читают предварительно загруженный кэш |

---

## 3. Аварийные работы — особый режим

**Аварийные работы** — это внеплановые работы с наивысшим приоритетом (например, аварийный ремонт трубопровода, срочное восстановление оборудования).

### Как они отличаются от плановых

| Параметр | Плановые работы | Аварийные работы |
|----------|----------------|-----------------|
| Файл загрузки | `import-requirements` | `import-emergency` |
| Поле в БД | `is_emergency = False` | `is_emergency = True` |
| Позиция в очереди | После всех аварийных | ВСЕГДА первыми |
| Сортировка | Дата начала → Приоритет | ТОЛЬКО дата начала |
| Поле `prioritet` | Учитывается | Игнорируется |

### Почему для аварийных нет приоритета

Аварийная работа = уже ЧП. Когда горит труба — не время делить «приоритет 1» и «приоритет 2». Все аварийные работы равнозначны по срочности, и порядок их обработки определяется только тем, **когда** началась авария.

### Загрузка аварийных работ

```bash
# CLI
maps import-emergency data/emergency_works.xlsx

# Веб: Импорт → «Аварийные работы» → выбрать файл
```

Если аварийная работа уже есть в БД (загружена через `import-requirements`), повторный `import-emergency` обновит её флаг `is_emergency = True` без дублирования.

---

## 4. Архитектура и структура проекта

```
PythonProject/
│
├── app/                            ← Основное приложение
│   ├── api/                        ← FastAPI веб-сервер (Этап 2)
│   │   ├── main.py                 ← FastAPI-приложение, подключение роутеров
│   │   ├── routes/
│   │   │   ├── status.py           ← GET /api/status
│   │   │   ├── allocation.py       ← POST /api/allocate, GET /api/sessions
│   │   │   ├── import_.py          ← POST /api/import/* (загрузка Excel)
│   │   │   └── export.py           ← GET /api/export/{session_id}
│   │   └── templates/
│   │       └── index.html          ← HTML-дашборд (Bootstrap 5)
│   │
│   ├── core/                       ← Ядро: конфигурация и логирование
│   │   ├── config.py               ← Настройки из .env (БД, пути, уровень лога)
│   │   └── logging_config.py       ← Логи в файл + консоль одновременно
│   │
│   ├── db/                         ← База данных
│   │   ├── database.py             ← Подключение к PostgreSQL, фабрика сессий
│   │   └── models.py               ← 13 таблиц БД (ORM-модели SQLAlchemy)
│   │
│   ├── models/                     ← Схемы данных (Pydantic)
│   │   └── schemas.py              ← Валидация строк при импорте из Excel
│   │
│   ├── repositories/               ← Слой работы с БД (паттерн Repository)
│   │   ├── base.py                 ← Базовый CRUD: get, add, delete, count
│   │   ├── work_repository.py      ← Работы, потребности (включая аварийные)
│   │   └── material_repository.py  ← Материалы, партии, поставки, списания, выданное
│   │
│   ├── allocation/                 ← Алгоритм распределения
│   │   └── engine.py               ← Движок: 5 слоёв, FIFO, кэши производительности
│   │
│   └── services/                   ← Сервисы ввода/вывода
│       ├── import_service.py       ← Excel → PostgreSQL (6 типов файлов)
│       └── export_service.py       ← PostgreSQL → Excel (4 листа)
│
├── tests/                          ← Автоматические тесты (pytest, 38 тестов)
│   ├── conftest.py                 ← Фикстуры: pg_engine, session, helpers
│   ├── test_engine.py              ← 17 тестов AllocationEngine (все 5 слоёв)
│   ├── test_repositories.py        ← 13 тестов репозиториев (запросы, сортировка)
│   └── test_allocation.py          ← 8 тестов схем Pydantic и EPSILON
│
├── migrations/                     ← Alembic-миграции схемы БД
│   ├── env.py                      ← Конфигурация Alembic
│   └── versions/
│       └── 9aff7eeed93d_initial_schema.py  ← Начальная схема (все 13 таблиц)
│
├── scripts/
│   └── generate_test_scenarios.py  ← Генерация тестовых Excel-файлов (12 сценариев)
│
├── data/
│   ├── templates/                  ← Шаблоны Excel для заполнения
│   └── exports/                    ← Результаты (создаются автоматически)
│
├── logs/                           ← Файлы логов (создаются автоматически)
│
├── main.py                         ← Точка входа, CLI-команды (Typer)
├── alembic.ini                     ← Конфигурация Alembic
├── pyproject.toml                  ← Конфигурация проекта, зависимости, ruff, pytest
├── requirements.txt                ← Список зависимостей (для pip install -r)
├── .env.example                    ← Шаблон .env (скопировать и заполнить)
├── .pre-commit-config.yaml         ← Git-хуки: ruff перед каждым коммитом
└── .gitignore                      ← Что не попадает в git
```

### Принцип слоёной архитектуры

```
Browser / CLI (main.py)
     ↓  вызывает
FastAPI (app/api/)          ← Этап 2: веб-интерфейс и REST API
     ↓  вызывает
Services (import_service, export_service)
     ↓  вызывает
Allocation Engine (engine.py)
     ↓  вызывает
Repositories (work_repository, material_repository)
     ↓  работает с
Database (SQLAlchemy ORM → PostgreSQL)
```

Каждый слой знает только о слое ниже. Это позволяет:
- Менять реализацию слоя без изменения остальных
- Тестировать каждый слой отдельно
- Добавлять новые интерфейсы (REST API, Telegram-бот) без изменения алгоритма

---

## 5. Технологический стек

| Компонент | Технология | Зачем |
|-----------|------------|-------|
| Язык | Python 3.12+ | Основной язык |
| БД | PostgreSQL 14+ | Надёжное хранилище, масштаб 1М+ строк |
| ORM | SQLAlchemy 2.0 | Работа с БД через Python-классы без сырого SQL |
| Миграции | Alembic 1.13+ | Изменение схемы БД без потери данных |
| Валидация | Pydantic v2 | Проверка типов и форматов при импорте |
| Excel | pandas + openpyxl | Импорт/экспорт .xlsx |
| Веб API | FastAPI 0.110+ | REST API, автодокументация Swagger |
| Веб-сервер | uvicorn | ASGI-сервер для FastAPI |
| Шаблоны | Jinja2 | HTML-дашборд (рендер на сервере) |
| CLI | Typer + Rich | Красивый интерфейс командной строки |
| Тесты | pytest 9.0+ (38 тестов) | Автоматические тесты с изоляцией PostgreSQL |
| Линтер | ruff | Проверка + форматирование кода |
| Git-хуки | pre-commit | Автоматическая проверка перед коммитом |

---

## 6. Быстрый старт

### Вариант А — Веб-интерфейс (рекомендуется)

```bash
# 1. Клонировать, создать окружение, установить зависимости
git clone https://github.com/your-org/maps.git && cd maps
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"

# 2. Настроить .env
cp .env.example .env
nano .env   # указать параметры PostgreSQL

# 3. Создать таблицы в БД
maps init

# 4. Запустить веб-сервер
maps serve

# 5. Открыть браузер → http://localhost:8000
#    - Загрузить Excel-файлы через вкладку «Импорт»
#    - Нажать «Запустить распределение»
#    - Скачать Excel-отчёт по кнопке «Скачать» в таблице сессий
```

### Вариант Б — Командная строка

```bash
# После установки (шаги 1-3 те же):

maps import-requirements data/requirements.xlsx
maps import-emergency data/emergency.xlsx        # если есть аварийные работы
maps import-stock data/stock.xlsx
maps import-supplies data/supplies.xlsx          # если есть поставки
maps import-writeoffs data/writeoffs.xlsx        # если есть списания из SAP
maps import-issued data/issued.xlsx              # если есть выдано не списано

maps status          # проверить загруженные данные
maps allocate        # запустить распределение
maps export <SESSION_ID>   # создать Excel-отчёт
```

---

## 7. Подробная инструкция по установке

### Требования к системе

- **Python 3.12 или выше**
  ```bash
  python --version    # Должно быть Python 3.12+
  ```

- **PostgreSQL 14 или выше**
  ```bash
  psql --version      # Должно быть 14+
  ```

### Создание базы данных PostgreSQL

```sql
-- Подключиться к PostgreSQL под суперпользователем
psql -U postgres

-- Создать пользователя системы
CREATE USER maps_user WITH PASSWORD 'придумайте_пароль';

-- Создать базу данных
CREATE DATABASE maps_db OWNER maps_user;

-- Дать права
GRANT ALL PRIVILEGES ON DATABASE maps_db TO maps_user;

\q
```

Или, если PostgreSQL настроен на Unix-сокеты (например, на Linux под вашим пользователем):

```bash
createdb maps_db
# В этом случае .env будет: DATABASE_URL=postgresql+psycopg2://dan@/maps_db?host=/var/run/postgresql
```

### Заполнение .env файла

Скопируйте шаблон и заполните параметры:

```bash
cp .env.example .env
```

```bash
# Два варианта задания параметров БД:

# Вариант 1 — Отдельные переменные (рекомендуется):
DB_HOST=localhost
DB_PORT=5432
DB_NAME=maps_db
DB_USER=maps_user
DB_PASSWORD=ваш_пароль

# Вариант 2 — Полный URL (для нестандартных конфигураций):
DATABASE_URL=postgresql+psycopg2://maps_user:пароль@localhost:5432/maps_db

# Настройки приложения
APP_ENV=development        # В production меняем на "production"
LOG_LEVEL=INFO             # DEBUG — подробно, INFO — стандартно
DATA_DIR=data              # Папка с Excel-файлами
EXPORT_DIR=data/exports    # Куда сохранять Excel-отчёты
```

### Создание таблиц (первый запуск)

```bash
# Вариант 1: через CLI (использует create_all, быстро)
maps init

# Вариант 2: через Alembic (рекомендуется для production)
alembic upgrade head
```

Если таблицы уже существуют — ни один из методов не затрёт данные.

### Настройка pre-commit (один раз)

```bash
pre-commit install
# Теперь ruff будет проверять код автоматически перед каждым git commit
```

---

## 8. Веб-интерфейс и API

### Запуск веб-сервера

```bash
# Стандартный запуск (доступен с других устройств в сети)
maps serve

# На конкретном порту
maps serve --port 9000

# Режим разработки (перезапуск при изменении файлов)
maps serve --reload

# Только локально (не доступен снаружи)
maps serve --host 127.0.0.1
```

### Дашборд (`http://localhost:8000`)

Главная страница показывает:

| Блок | Что показывает |
|------|----------------|
| Карточки статуса | Активных работ / Материалов / Партий на складе / Последний запуск |
| Кнопка «Запустить» | Запускает алгоритм в фоне, таблица обновляется автоматически каждые 3 сек |
| Таблица сессий | Время, статус (зелёный/жёлтый/красный), статистика, кнопка «Скачать» |
| Импорт файлов | Аккордеон с 6 формами загрузки — выбрал файл → импорт начался автоматически |

### Скачивание отчёта с рабочего компьютера

После завершения распределения кнопка «Скачать» в таблице ведёт на URL:
```
http://192.168.1.100:8000/api/export/20260523_143055_a1b2c
```

Этот URL можно:
- Скопировать (правая кнопка мыши → «Копировать ссылку»)
- Открыть с любого устройства в той же сети
- Отправить себе в мессенджер и открыть с рабочего компьютера

По ссылке браузер сразу скачает `.xlsx`-файл без лишних шагов.

### REST API

Все операции доступны как REST API (для интеграции с другими системами):

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/status` | Статистика системы (JSON) |
| `POST` | `/api/allocate` | Запустить распределение |
| `GET` | `/api/sessions` | Список сессий (`?limit=20`) |
| `GET` | `/api/sessions/{id}` | Детали одной сессии |
| `POST` | `/api/import/requirements` | Загрузить потребности |
| `POST` | `/api/import/emergency` | Загрузить аварийные работы |
| `POST` | `/api/import/stock` | Загрузить остатки склада |
| `POST` | `/api/import/supplies` | Загрузить поставки |
| `POST` | `/api/import/writeoffs` | Загрузить списания |
| `POST` | `/api/import/issued` | Загрузить выдано не списано |
| `GET` | `/api/export/{session_id}` | Скачать Excel-отчёт |

Пример запуска распределения через curl:
```bash
curl -X POST http://localhost:8000/api/allocate
# → {"status": "started", "message": "Распределение запущено"}

# Проверить статус через 10 секунд:
curl http://localhost:8000/api/sessions | python3 -m json.tool
```

Пример загрузки файла через curl:
```bash
curl -X POST http://localhost:8000/api/import/stock \
     -F "file=@data/stock.xlsx"
# → {"status": "ok", "file": "stock.xlsx", "stats": {"created": 1200, "errors": 0}}
```

### Автодокументация API (`http://localhost:8000/docs`)

FastAPI автоматически генерирует интерактивную документацию Swagger UI. Там можно:
- Посмотреть все эндпоинты с описанием
- Попробовать запросы прямо в браузере
- Скачать OpenAPI-схему для интеграций

---

## 9. Формат входных данных Excel

### Файл потребностей (`import-requirements`)

Каждая строка = потребность одной работы в одном материале.
Одна работа может занимать несколько строк (разные материалы).

| Колонка | Тип | Обязательна | Пример | Описание |
|---------|-----|:-----------:|--------|----------|
| Код работы | Текст | ✅ | `WO-001` | Уникальный код работы |
| Системный номер | Текст | ✅ | `000000000010010001` | SAP-номер материала |
| Потребность | Число | ✅ | `150.5` | Нужное количество (строго > 0) |
| Прогнозная цена | Число | | `15000.00` | Цена единицы для расчёта обеспечённости |
| Тип работы | Текст | | `Ремонт` | Тип/вид работы |
| Филиал | Текст | | `Алматы` | Филиал компании |
| Подразделение | Текст | | `Цех 3` | Подразделение |
| Центр затрат | Текст | | `CC-KZ01-101` | Центр затрат SAP |
| Завод | Текст | | `KZ01` | Код завода (Plant в SAP) |
| Дата начала | Дата | | `2026-06-01` | Формат YYYY-MM-DD |
| Дата окончания | Дата | | `2026-07-31` | Должна быть ≥ даты начала |
| Приоритет | Число | | `2` | 1 (высший), 2 (средний), 3 (низший) |
| Наименование материала | Текст | | `Кабель ВВГ 3х2,5` | Для справки |
| Ед.изм | Текст | | `м` | Единица измерения |

> **Прогнозная цена** — важна для расчёта обеспечённости по стоимости. Если не указана, считается обеспечённость по количеству.

---

### Файл аварийных работ (`import-emergency`)

Формат идентичен файлу потребностей. Отличие одно: все загруженные работы получают флаг `is_emergency = True` и автоматически становятся во главе очереди.

> ⚠️ **Важно:** Если аварийная работа уже есть в БД (загружена через `import-requirements`), повторная загрузка через `import-emergency` установит ей флаг аварийности без дублирования записи.

---

### Файл складских остатков (`import-stock`)

Каждая строка = одна партия материала на одном складе.

> ⚠️ **Внимание:** При импорте все существующие остатки **полностью заменяются**. Это «снимок склада» на текущий момент.

| Колонка | Тип | Обязательна | Пример |
|---------|-----|:-----------:|--------|
| Код склада | Текст | ✅ | `SKL-ALM-01` |
| Системный номер | Текст | ✅ | `000000000010010001` |
| Количество | Число | ✅ | `500.0` |
| Номер партии | Текст | | `P-2026-001` |
| Стоимость за ед | Число | | `1500.00` |
| Дата поступления | Дата | | `2026-01-15` |
| Тип склада | Текст | | `центральный` |
| Филиал склада | Текст | | `Алматы` |
| Завод склада | Текст | | `KZ01` |
| Наименование материала | Текст | | `Кабель ВВГ 3х2,5` |
| Ед.изм | Текст | | `м` |
| Группа материала | Текст | | `Кабели` |

---

### Файл поставок (`import-supplies`)

Каждая строка = один материал в одной поставке.

> ⚠️ **Внимание:** При импорте все существующие поставки **полностью заменяются**.

| Колонка | Тип | Обязательна | Пример |
|---------|-----|:-----------:|--------|
| Системный номер | Текст | ✅ | `000000000010010001` |
| Количество | Число | ✅ | `1000.0` |
| Договор | Текст | | `ДОГ-2026-001` |
| Поставщик | Текст | | `ТОО Стройснаб-KZ` |
| Код склада | Текст | | `SKL-ALM-01` |
| Филиал | Текст | | `Алматы` |
| Завод | Текст | | `KZ01` |
| Дата поставки | Дата | | `2026-06-15` |
| Стоимость за ед | Число | | `1400.00` |
| Статус | Текст | | `confirmed` |

Допустимые статусы: `confirmed` (подтверждено), `in_transit` (груз отправлен).

---

### Файл списаний (`import-writeoffs`)

Данные о фактических списаниях материалов на работы из SAP. Соответствует **Слою 1** алгоритма.

> ⚠️ **Внимание:** При импорте все существующие записи списаний **полностью заменяются**.

| Колонка | Тип | Обязательна | Пример | Описание |
|---------|-----|:-----------:|--------|----------|
| Код работы | Текст | ✅ | `WO-001` | Работа, на которую списан материал |
| Системный номер | Текст | ✅ | `000000000010010001` | Материал |
| Количество | Число | ✅ | `50.0` | Фактически списанное количество |
| Стоимость за ед | Число | | `1500.00` | Фактическая стоимость единицы |
| Сумма | Число | | `75000.00` | Общая сумма списания |
| Номер документа | Текст | | `СП-000001` | Номер акта/документа списания |
| Дата списания | Дата | | `2026-03-01` | Дата проведения документа |

> **Если работа не найдена** в БД при импорте — строка пропускается с предупреждением в лог.

---

### Файл выдано не списано (`import-issued`)

Материалы, которые физически выданы (находятся у рабочих), но документально ещё не списаны. Соответствует **Слою 2** алгоритма.

> ⚠️ **Внимание:** При импорте все существующие записи **полностью заменяются**.

| Колонка | Тип | Обязательна | Пример | Описание |
|---------|-----|:-----------:|--------|----------|
| Код работы | Текст | ✅ | `WO-001` | Работа, для которой выдан материал |
| Системный номер | Текст | ✅ | `000000000010010001` | Материал |
| Количество | Число | ✅ | `40.0` | Физически выданное количество |
| Стоимость за ед | Число | | `1500.00` | Стоимость на момент выдачи |
| Сумма | Число | | `60000.00` | Общая сумма |
| Код склада | Текст | | `SKL-ALM-01` | Информационно — откуда выдали |
| Дата выдачи | Дата | | `2026-03-15` | Дата физической выдачи |

---

## 10. Команды CLI

После установки (`pip install -e .`) команды доступны без `python main.py`:

### `maps init`

Создаёт все таблицы в PostgreSQL. Запускать **один раз** при первом запуске.
Безопасно вызывать повторно — существующие данные не удаляет.

```bash
maps init
```

---

### `maps serve`

Запускает FastAPI веб-сервер с дашбордом.

```bash
maps serve                    # на всех интерфейсах, порт 8000
maps serve --port 9000        # другой порт
maps serve --host 127.0.0.1   # только локально
maps serve --reload           # автоперезагрузка при изменении кода (для разработки)
```

---

### `maps import-requirements FILE`

Импортирует потребности работ из Excel.

```bash
maps import-requirements data/requirements.xlsx
maps import-requirements data/requirements.xlsx --sheet "Потребности"
```

---

### `maps import-emergency FILE`

Импортирует аварийные работы. Все работы в файле получают `is_emergency = True`.

```bash
maps import-emergency data/emergency_works.xlsx
```

---

### `maps import-stock FILE`

Импортирует складские остатки. **Полностью заменяет** существующие остатки.

```bash
maps import-stock data/stock.xlsx
```

---

### `maps import-supplies FILE`

Импортирует поставки. **Полностью заменяет** существующие поставки.

```bash
maps import-supplies data/supplies.xlsx
```

---

### `maps import-writeoffs FILE`

Импортирует списания из SAP (Слой 1). **Полностью заменяет** существующие записи.

```bash
maps import-writeoffs data/writeoffs.xlsx
maps import-writeoffs data/writeoffs.xlsx --sheet "Списания"
```

---

### `maps import-issued FILE`

Импортирует «Выдано не списано» (Слой 2). **Полностью заменяет** существующие записи.

```bash
maps import-issued data/issued.xlsx
```

---

### `maps allocate`

Запускает алгоритм распределения. Каждый запуск — новая сессия с уникальным ID.

```bash
maps allocate
maps allocate --session "сценарий_июнь_2026"
```

Пример вывода:
```
╭─────────────────────────────────────────────╮
│  MAPS — Распределение                       │
│  Слои: Списание → Выдано → Склад →         │
│        Поставки → К закупу                  │
╰─────────────────────────────────────────────╯
✓ Распределение завершено!
  ID сессии: 20260523_143000_abc12

  ┌────────────────────────┬───────┐
  │ Обработано потребностей│   800 │
  │ Строк распределения    │   735 │
  │ Позиций дефицита       │    65 │
  └────────────────────────┴───────┘

Для экспорта: maps export 20260523_143000_abc12
```

---

### `maps export SESSION_ID`

Создаёт Excel-отчёт с 4 листами.

```bash
maps export 20260523_143000_abc12
maps export 20260523_143000_abc12 --output /tmp/reports
```

---

### `maps reset-data`

⚠️ **Осторожно!** Полностью очищает все таблицы БД. Используется только для сброса к чистому состоянию перед повторной загрузкой всех данных.

```bash
maps reset-data
```

---

### `maps status`

Показывает текущее состояние БД: количество записей и последние сессии.

```bash
maps status
```

---

## 11. Структура базы данных

### Входные таблицы (заполняются при импорте)

| Таблица | Описание | Ключевые поля |
|---------|----------|---------------|
| `works` | Строительно-монтажные работы | `is_emergency`, `prioritet`, `data_nachala` |
| `materials` | Справочник материалов | `sys_nomer` — уникальный номер SAP |
| `requirements` | Потребности: работа + материал + количество | `prognosnaya_tsena`, `raspredeleno`, `deficit` |
| `warehouses` | Справочник складов | `filial`, `zavod` — для приоритизации |
| `stock_batches` | Партии материалов на складах | `data_postupleniya` для FIFO, `dostupno` |
| `supplies` | Заголовки поставок | `filial` — ключевой фильтр |
| `supply_lines` | Строки поставок (материал + кол-во) | `dostupno` — доступное для резервирования |
| `writeoffs` | Списания из SAP (Слой 1) | `nomer_dokumenta`, `data_spisaniya` |
| `issued_not_written_off` | Выдано не списано (Слой 2) | `data_vydachi` |

### Выходные таблицы (заполняются алгоритмом)

| Таблица | Описание |
|---------|----------|
| `allocation_sessions` | История запусков: ID, статус, время, статистика |
| `allocation_results` | Строки распределения по слоям (`istochnik`) |
| `stock_movements` | Детальные движения по партиям склада |
| `deficit_records` | Позиции дефицита с оценочной стоимостью |

### Значения поля `istochnik` (источник покрытия)

| Значение | Слой | Описание | `is_possible` |
|----------|------|----------|:---:|
| `spisanie` | 1 | Списание из SAP | `False` |
| `vydano` | 2 | Выдано физически, не списано | `False` |
| `sklad` | 3 | Склад своего завода/филиала | `False` |
| `vozmozhnoe_sklad` | 3б | Склад другого филиала (только анализ) | `True` |
| `postavka` | 4 | Поставка в пути (своего филиала) | `False` |

### Схема связей

```
works ──────────────── requirements ──── materials
  │                         │                │
  │ (is_emergency)          │           stock_batches ── warehouses
  │                         │                               │
  ├── writeoffs ────────────┤                          supplies ── supply_lines
  │                         │
  └── issued_not_written_off┤
                            │
allocation_sessions ────────┤
        │                   │
        ├── allocation_results (istochnik: 5 типов)
        ├── stock_movements
        └── deficit_records
```

---

## 12. Результаты работы системы

### Excel-отчёт (4 листа)

Создаётся командой `maps export SESSION_ID` или кнопкой «Скачать» в веб-дашборде.

---

#### Лист 1: «Распределение» — широкая сводная таблица

Каждая строка = одна потребность (работа + материал). Все 5 слоёв — в виде колонок.

| Группа колонок | Что показывает |
|----------------|----------------|
| **Реквизиты работы** | Код, тип, филиал, завод, приоритет, аварийность, даты |
| **Реквизиты материала** | Системный номер, наименование, ед.изм |
| **Потребность** | Количество, прогнозная цена, плановая стоимость |
| **Слой 1: Списание** | Количество, сумма |
| **Слой 2: Выдано** | Количество, сумма |
| **Слой 3: Склад** | Количество, средняя цена, сумма |
| **Слой 4: Поставка** | Количество, сумма, договора, поставщики |
| **Дефицит (к закупу)** | Количество, оценочная стоимость |
| **Итоговое покрытие** | Закрыто кол-во, закрытая стоимость, % обеспечённости |

---

#### Лист 2: «Движение склада»

Детальные движения по каждой партии. Для проверки расчётов и бухгалтерского учёта.

---

#### Лист 3: «Остатки складов»

Итоговые остатки: сколько было → сколько зарезервировано → сколько осталось.

---

#### Лист 4: «Возможное перемещение»

Материалы на складах других филиалов, которые могут закрыть часть дефицита при межфилиальной переброске.

---

## 13. Разработка и тестирование

### Тестовая БД

Для тестов используется **отдельная база** `maps_test_db` (не рабочая `maps_db`).

```bash
# Создать тестовую БД (один раз)
createdb maps_test_db
```

Изоляция тестов: каждый тест запускается в отдельной транзакции, которая **откатывается** после завершения. Данные в БД не сохраняются — тесты независимы друг от друга.

### Запуск тестов

```bash
# Все тесты (38 штук)
pytest

# Подробный вывод
pytest -v

# Только тесты движка
pytest tests/test_engine.py

# Только тесты репозиториев
pytest tests/test_repositories.py

# Конкретный тест по имени
pytest -k "test_fifo"
pytest -k "test_emergency"

# С отчётом покрытия кода
pytest --cov=app --cov-report=html
# Открыть: htmlcov/index.html
```

### Что покрыто тестами

| Файл | Кол-во | Что тестируется |
|------|:------:|-----------------|
| `test_engine.py` | 17 | AllocationEngine: все 5 слоёв, FIFO, завод→филиал, аварийный приоритет, повторный запуск |
| `test_repositories.py` | 13 | Репозитории: сортировка очереди, группировка партий, сброс результатов |
| `test_allocation.py` | 8 | Схемы Pydantic, константа EPSILON |
| **Итого** | **38** | — |

### Ключевые тесты

```python
# Тест исправления основного бага (reset_dostupno):
# Без исправления второй запуск давал пустые склады и дефицит там, где его нет
def test_repeated_run_gives_same_result(session):
    ...
    run_engine(session, session_id="test_run1")
    run_engine(session, session_id="test_run2")
    assert batch.dostupno == Decimal("20")  # 100 - 80 = 20 после КАЖДОГО запуска

# Тест приоритета аварийных работ:
def test_emergency_work_gets_stock_before_normal(session):
    # На складе 50 шт — хватит только одной работе
    # Обычная начинается раньше, аварийная — позже
    # Но аварийная должна получить первой!
    ...
    assert len(get_deficits(session, normal_work.id)) == 1  # обычная — в дефиците
```

### Проверка и форматирование кода

```bash
ruff check .           # проверить ошибки стиля
ruff check . --fix     # исправить автоматически
ruff format .          # отформатировать код
pre-commit run --all-files  # запустить все хуки
```

### Alembic: миграции схемы БД

```bash
# Применить все миграции (первый деплой или обновление)
alembic upgrade head

# Посмотреть текущую версию
alembic current

# Создать новую миграцию после изменения models.py
alembic revision --autogenerate -m "добавить таблицу xxx"
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1
```

---

## 14. Дорожная карта

### ✅ Этап 1 — MVP (выполнен)

- [x] Архитектура проекта с чистым разделением слоёв
- [x] Подключение к PostgreSQL (SQLAlchemy 2.0)
- [x] Все ORM-модели (13 таблиц)
- [x] 5-слойный алгоритм распределения (все слои)
- [x] Аварийные работы: наивысший приоритет, отдельный файл
- [x] Прогнозная цена: обеспечённость по стоимости
- [x] FIFO + приоритет завод → филиал в слое 3
- [x] Фильтрация поставок по филиалу работы (Слой 4)
- [x] Возможное перемещение из других филиалов (3б)
- [x] Импорт из Excel: 6 типов файлов
- [x] Экспорт в Excel: 4 листа с результатами
- [x] Alembic: миграции схемы БД
- [x] Автотесты: 38 тестов с изоляцией PostgreSQL
- [x] CLI-интерфейс (Typer + Rich)
- [x] Логирование в файл + консоль

### ✅ Этап 2 — Веб-интерфейс (выполнен)

- [x] FastAPI: REST API для всех операций
- [x] Веб-дашборд: статус, запуск распределения, таблица сессий
- [x] Загрузка Excel через браузер (multipart upload)
- [x] Скачиваемые ссылки на Excel-отчёты (для использования с рабочего ПК)
- [x] Автообновление статуса при выполнении распределения
- [x] Автодокументация API (Swagger UI `/docs`)

### 🔄 Этап 3 — Production (следующий)

- [ ] Деплой на сервер (Oracle Cloud Ubuntu + Cloudflare Tunnel)
- [ ] CI/CD через GitHub Actions (тесты при каждом push)
- [ ] Фильтрация распределения по конкретному филиалу/заводу
- [ ] Сценарное моделирование: несколько именованных версий распределения
- [ ] Интеграция с SAP (чтение данных через SAP GUI Scripting или RFC)
- [ ] Расширенный аудит: кто запустил, какие файлы загрузил, changelog

### 🔮 Этап 4 — Optimization Engine

- [ ] Критический путь (CPM — Critical Path Method)
- [ ] Граф конфликтов ресурсов (NetworkX)
- [ ] Оптимизация распределения (Google OR-Tools)
- [ ] Анализ рисков поставок (вероятность задержки)

### 🤖 Этап 5 — AI System

- [ ] AI-агент на LangGraph
- [ ] RAG: поиск по истории распределений и документам
- [ ] Прогнозирование дефицита на основе исторических данных
- [ ] Объяснение причин дефицита на естественном языке
- [ ] Чат с AI-агентом для анализа и рекомендаций

---

## 15. Частые вопросы

**Q: Почему PostgreSQL, а не SQLite?**

PostgreSQL рассчитан на параллельную работу нескольких пользователей и большие объёмы (1 млн+ строк). SQLite работает только с одним пользователем одновременно и не подходит для production. Кроме того, MAPS использует bulk UPDATE и оконные функции, которые быстрее работают в PostgreSQL.

---

**Q: Почему `Decimal`, а не `float` для количеств и стоимостей?**

`float` имеет ошибки округления: `0.1 + 0.2 = 0.30000000000000004`. В финансовых расчётах это приводит к расхождениям. `Decimal` точен до указанного знака и корректно работает с бухгалтерией.

---

**Q: Что такое FIFO и зачем он нужен?**

First In First Out — «первым пришёл, первым ушёл». Партия, которая поступила на склад раньше, списывается первой. Это стандартный метод учёта (МСФО IAS 2) и минимизирует риск «залёживания» старого материала.

---

**Q: Зачем нужны слои 1 и 2, если материал уже использован?**

Они показывают **реальную картину**:
- Слой 1 (Списание): материал официально «закрыт» в SAP — зафиксированный факт
- Слой 2 (Выдано): материал у рабочих — они уже работают — это тоже покрытие

Без этих слоёв алгоритм думал бы, что потребность не покрыта, и дважды пытался бы выделить материал со склада.

---

**Q: Почему поставки фильтруются по филиалу, а не по заводу?**

Поставка — это логистическая единица, привязанная к точке доставки (филиалу). Один филиал может включать несколько заводов. Поставка адресована в филиал как логистический адрес, а не в конкретный завод.

---

**Q: Что такое «сессия распределения»?**

Каждый запуск `maps allocate` создаёт новую сессию с уникальным ID (например, `20260523_143000_abc12`). Это позволяет хранить историю нескольких запусков. Например: «что если изменить данные склада?» — меняем, запускаем новую сессию, сравниваем результаты с предыдущей.

---

**Q: Почему распределение запускается в фоне через веб-интерфейс?**

Распределение может занимать 10-60 секунд (зависит от количества потребностей). Если запускать синхронно — браузер будет ждать без ответа и может сбросить соединение. FastAPI запускает распределение в фоновой задаче (`BackgroundTasks`), сразу возвращает ответ `{"status": "started"}`, а дашборд автоматически опрашивает `/api/sessions` каждые 3 секунды и показывает обновлённый статус.

---

**Q: Как адаптировать колонки Excel под свой формат?**

Откройте `app/services/import_service.py` и найдите словари соответствия колонок (например, `REQUIREMENTS_COLUMN_MAP`). Измените ключи словарей (левая часть — имена ваших колонок в Excel). Значения (правая часть — внутренние имена системы) не менять.

---

**Q: Что означает «Возможное перемещение» в отчёте?**

Это склады **других филиалов**, на которых есть нужный материал. Алгоритм фиксирует их как `vozmozhnoe_sklad` — возможный, но незадействованный источник. Это подсказка для логиста: «если везти из Астаны в Алматы — вот откуда и сколько можно взять».

---

**Q: Можно ли запустить несколько сценариев одновременно?**

Запускать несколько `maps allocate` одновременно не рекомендуется — они конкурируют за обновление одних и тех же полей (`raspredeleno`, `dostupno`). Запускайте поочерёдно. Каждая сессия сохраняется независимо — можно сравнивать результаты разных запусков.

---

*Документация актуальна для версии 2.0.0 (FastAPI + Web UI) | Обновлено: 2026-05-23*
